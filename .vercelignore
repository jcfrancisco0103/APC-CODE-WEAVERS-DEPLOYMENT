# Exclude local virtualenvs and large build artifacts
pbl/
.venv/
venv/
env/
env.bak/

# Python caches and logs
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.dll
*.log
logs/

# Local heavy artifacts (do NOT exclude app static assets)
# Keep media/ and static/ so WhiteNoise can serve them in serverless
# If you use a CDN or cloud storage later, you can re-exclude as needed

# Large archives and binaries
ecommerce.zip
*.zip
*.tar
*.tar.gz
*.tgz

# VCS and CI metadata
.git/
.gitignore
.gitmodules
.github/

# Other common heavy folders
node_modules/
dist/
build/
coverage/
*.ipynb
.DS_Store
